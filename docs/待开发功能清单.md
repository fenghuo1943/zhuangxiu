# 装修手记 — 待开发功能清单

> 最后更新：2026-07-10

---

## 🔴 高优先级

### ~~H1. 首页按钮改用 React Router 导航~~ ✅ 已完成 (2026-07-10)
- **文件**: `src/components/dashboard/ReminderCard.tsx`
- 将 `window.location.href` 替换为 `useNavigate()` 客户端路由切换

### ~~H2. 记账页新增编辑已有记录功能~~ ✅ 已完成 (2026-07-10)
- **文件**: `src/pages/ExpensePage.tsx`, `src/data/store.ts`
- 新增 `updateExpense()` store 函数（支持修改金额时自动重算分类支出）
- Modal 支持新增/编辑双模式，复用同一表单，每条记录增加编辑按钮

### ~~H3. ExpensePage CSV 导入导出~~ ✅ 已完成 (2026-07-10)
- **文件**: `src/pages/ExpensePage.tsx`
- CSV 导出：带 BOM (Excel 兼容 UTF-8)、表头、双引号转义
- CSV 导入：自动检测表头、行级错误校验（金额无效/标题为空/列数不足）、错误信息汇总提示
- 导出按钮改为下拉菜单（CSV / JSON）
- 导入增加独立 JSON 和 CSV 按钮

---

## 🟡 中优先级

### ~~M1. FlowPage 编辑顺序功能~~ ✅ 已完成 (2026-07-10)
- **文件**: `src/pages/FlowPage.tsx`, `src/data/store.ts`, `src/data/types.ts`
- 新增 `flowCustomOrder` 状态 + `setFlowCustomOrder` / `getOrderedFlowSteps` store 函数
- "编辑顺序"按钮切换编辑模式，每个步骤显示 ⬆⬇ 移动按钮
- 支持"恢复默认"重置顺序

### ~~M2. 比价页同步到待购清单~~ ✅ 已完成 (2026-07-10)
- **文件**: `src/pages/ComparePage.tsx`, `src/data/store.ts`, `src/components/dashboard/PurchaseSummary.tsx`
- 每个型号新增"同步到待购"按钮，已同步显示绿色 ✓
- 首页待购材料/ReminderCard 待购计数包含已同步的比价型号

### ~~M3. 移动端 3 摘要卡片适配~~ ✅ 已完成 (2026-07-10)
- **文件**: `src/components/dashboard/TodayFocus.tsx`, `src/styles/components.css`
- 桌面端显示完整 TodayFocus（优先处理 + 4 统计格）
- 移动端 (<768px) 隐藏 TodayFocus，替换为 3 个独立摘要卡：预算余额 / 待办待购 / 最近支出
- 每个卡片带左侧彩色边框（coral/green/blue）

### ~~M4. 记账页子标签 chips 展示~~ ✅ 已完成 (2026-07-10)
- **文件**: `src/pages/ExpensePage.tsx`, `src/styles/components.css`
- 新增/编辑 Modal 增加"关联阶段"下拉选择
- 分类折叠列表内显示阶段 chips，点击筛选，再次点击取消
- 每条支出记录显示关联阶段标签（蓝色 tag）

---

## 🟢 低优先级

### ~~L1. 通用组件封装 (NumberInput / Select / DateInput)~~ ✅ 已完成 (2026-07-10)
- **文件**: `src/components/common/NumberInput.tsx`, `Select.tsx`, `DateInput.tsx`
- NumberInput: 仅允许合法数字输入、失焦校验、min/max 钳制
- Select: 统一 value/onChange 接口、支持 placeholder
- DateInput: 封装 type="date"、导出 `todayStr()` 工具函数

### ~~L2. 工具页真实功能实现~~ ✅ 已完成 (2026-07-10)
- **文件**: `src/pages/ToolsPage.tsx`
- 预算计算器: 面积×档次=总预算范围，自动按五类比例拆分
- 工期预估: 新房/旧房×面积系数=总工期+22/9阶段明细条
- 保留 2 个"即将上线"占位工具

### ~~L3. 多项目数据隔离~~ ✅ 已完成 (2026-07-10)
- **文件**: `src/data/store.ts`, `src/data/types.ts`, TodoPanel/ReminderCard/TodayFocus
- Todo 增加 `projectId` 字段，按活跃项目过滤显示
- 新增 `ProjectState` + `projectStates` 映射，切换项目时保存/恢复 budget 和 flowDoneStepIds

### ~~L4. 公告/账号页面~~ ✅ 已完成 (2026-07-10)
- **文件**: `src/pages/NoticesPage.tsx`, `AccountPage.tsx`, `src/App.tsx`
- 创建占位页面，注册 `/notices` 和 `/account` 路由

### ~~L5. 记账页撤销删除 (Toast + Undo)~~ ✅ 已完成 (2026-07-10)
- **文件**: `src/pages/ExpensePage.tsx`
- 删除操作显示底部 Toast：「已删除"xxx"，可撤销」
- 5 秒自动消失，点击撤销恢复数据

### ~~L6. 记账页导入预览机制~~ ✅ 已完成 (2026-07-10)
- **文件**: `src/pages/ExpensePage.tsx`
- CSV 导入先解析并在 Modal 表格中预览
- 显示有效行数 + 错误行详情，用户确认后批量写入

### ~~L7. 比价页 CSV 导入导出~~ ✅ 已完成 (2026-07-10)
- **文件**: `src/pages/ComparePage.tsx`
- CSV 导出: 品类/型号/规格/备注/数量/渠道/价格 (BOM + Excel兼容)
- CSV 导入: 批量创建品类和型号

### ~~L8. 移动端支出摘要卡~~ ✅ 已完成 (2026-07-10)
- 已包含在 M3 中：移动端 3 摘要卡片含「最近支出」

---

## ⚪ 远期 / 可选

### F1. 云同步
- 接入后端 API 或云存储
- 优先级：依赖后端就绪

### F2. 后端数据库
- 将 localStorage 替换为服务端持久化
- 优先级：依赖后端就绪

### F3. 用户认证系统
- 多用户登录、权限管理
- 优先级：产品化阶段

### F4. 资料详情页
- 点击流程页的关联资料（标准/验收/攻略/避坑）打开详情页或弹窗
- 优先级：内容就绪后

---

## 开发记录

| 日期 | 完成项 |
|------|--------|
| 2026-07-09 | 阶段一二三：项目基础 + 首页静态还原 + 基础交互 |
| 2026-07-10 | 阶段四：FlowPage / ExpensePage / PurchasePage / ComparePage / ToolsPage 全部模块页面 |
| 2026-07-10 | H1-H3 高优项：React Router 导航 / 记账编辑 / CSV 导入导出 |
| 2026-07-10 | M1-M4 中优项：流程编辑顺序 / 比价同步待购 / 移动端摘要卡 / 记账阶段 chips |
| 2026-07-10 | L1-L8 低优项：通用组件 / 工具实装 / 项目隔离 / 占位页面 / 撤销删除 / 导入预览 / 比价CSV / 移动摘要卡 |
